#include <stdio.h>

int main(){
    printf("The size taken by a char is %d\n", sizeof(char));
    printf("The size taken by a unsigned char is %d\n", sizeof(unsigned char));
    printf("The size taken by a short int is %d\n", sizeof(short int));
    return 0;
}